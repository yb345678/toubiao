# Backend Skills

This directory is reserved for backend-native Skill implementations.

The current backend orchestrator reuses the existing root-level `skills/`
modules to keep the demo runnable while preserving a clean replacement point.
